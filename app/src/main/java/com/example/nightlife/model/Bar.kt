package com.example.nightlife.model

class Bar(id: Int, name: String, rating: Double) {
    val id = id
    val name = name
    var rating = rating
    var open: Boolean = false
    // X and Y coordinates?
}